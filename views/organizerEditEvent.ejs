<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <title><%=page.title%></title>
  </head>
  <body>
    <div class="p-5 bg-primary text-white text-center">
      <h1><%=page.heading%></h1>
      <p><%=page.description%></p>
    </div>

    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" href="/organizer">Organizer Home</a>
          </li>
        </ul>
      </div>
    </nav>
    <div class="container mt-3">
      <form action="update_event" method="post">
        <div class="mb-3">
          <label for="title">Event Title</label>

          <!-- ############# -->
          <input type="text" class="form-control" id="create_title" value="<%=event.title %>" name="title" />
        </div>

        <div class="mb-3 mt-3">
          <label for="description">Description:</label>
          <input
            type="text"
            class="form-control"
            rows="5"
            id="create_desc"
            name="description"
            value="<%=event.description %>"
          />
        </div>
        <div class="mb-3 mt-3">
          <label for="date_event">Current Event Date: <%=event.date_event%></label>
          <input type="text" class="form-control" id="date_event" value="<%=event.date_event%>" name="date_event" />
        </div>
        <div class="mb-3 mt-3"></div>
        <label for="num_tickets">Number of Tickets</label>
        <input type="text" class="form-control" id="num_tickets" value="<%=event.num_tickets%>" name="num_tickets" />
        <div class="mb-3 mt-3">
          <input type="hidden" id="event_<%=event.id%>" name="id" value="<%=event.id%>" />
          <button type="submit" class="btn btn-primary">Update event</button>
        </div>
      </form>
    </div>
  </body>
</html>
